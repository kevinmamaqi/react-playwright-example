{
  "name": "github-graphql-api",
  "version": "1.0.0",
  "description": "## Structure",
  "main": "index.js",
  "scripts": {
    "app": "cp .env ./app/.env && cd app && npm run dev",
    "local-test": "cp .env ./e2e/.env && cd e2e && npx playwright test",
    "e2e-test": "cd ./scripts && source ./docker.sh; launchTest",
    "e2e-stop": "cd ./scripts && source ./docker.sh; stopDocker"
  },
  "keywords": [
    "react",
    "typescript",
    "playwright",
    "docker"
  ],
  "author": "Kevin Mamaqi Kapllani",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "1.21.1"
  }
}
