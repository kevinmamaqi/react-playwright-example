import styled from 'styled-components'
import { BaseComponentI } from '../../theme'
import Container from './Container'

const Footer = styled(Container)``

export default styled(Footer)<BaseComponentI>``
