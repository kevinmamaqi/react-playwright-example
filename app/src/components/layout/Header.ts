import styled from 'styled-components'
import { BaseComponentI } from '../../theme'
import Container from './Container'

const Header = styled(Container)``

export default styled(Header)<BaseComponentI>``
